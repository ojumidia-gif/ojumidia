import { ArrowRight, Camera, MapPin, Play, Volume2 } from "lucide-react";
import { useEffect, useMemo, useRef, useState } from "react";
import { Link, useLocation } from "wouter";
import { trpc } from "@/lib/trpc";
import { useEditorialLive } from "@/hooks/useEditorialLive";
import { OjuMark, PublicHeader } from "@/components/PublicHeader";
import { OjuMethod } from "@/components/OjuMethod";
import { portalContentDefaults, type HeroContent, usePortalContent } from "@/lib/portalContent";
import { PageMeta } from "@/components/PageMeta";
import { firebasePreviewAssets, isStaticFirebasePreview } from "@/lib/runtimeMode";

function FrontCard({ label, title, action, href }: { label: string; title: string; action: string; href: string }) {
  return <Link href={href} className="group relative min-h-36 overflow-hidden rounded border border-white/15 bg-[#17130f] p-5 text-white transition hover:border-[#ef9e59]/80"><div className="absolute inset-0 bg-[radial-gradient(circle_at_90%_20%,rgba(180,111,56,.45),transparent_30%),linear-gradient(110deg,rgba(17,14,11,.25),rgba(17,14,11,.85))]" /><div className="relative"><p className="text-[10px] font-bold uppercase tracking-[.14em] text-[#ef9e59]">{label}</p><h3 className="mt-3 max-w-[15rem] font-serif text-2xl leading-[1.05]">{title}</h3><span className="mt-5 inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[.08em] text-[#ef9e59]">{action} <ArrowRight className="h-4 w-4 transition group-hover:translate-x-1" /></span></div></Link>;
}
type LinkContent = { items: Array<{ label: string; href: string; external?: boolean }> };
type FeaturedContent = { eyebrow: string; title: string; emptyMessage: string; allLabel: string; allHref: string };
type EditorialFrontsContent = { items: Array<{ label: string; title: string; action: string; href: string }> };

export default function Home() {
  const { data: items } = trpc.editorial.featured.useQuery({}, { refetchInterval: 5000 });
  const { data: livingBackgrounds } = trpc.media.homeBackgrounds.useQuery(undefined, { refetchInterval: 30000 });
  const { data: heroTransition } = trpc.media.homeBackgroundConfig.useQuery(undefined, { refetchInterval: 30000 });
  const { data: activeAds } = trpc.commercial.activeAds.useQuery(undefined, { refetchInterval: 30000 });
  const [isMuted, setIsMuted] = useState(true); const [adminTapCount, setAdminTapCount] = useState(0); const [heroIndex, setHeroIndex] = useState(0); const adminTapAt = useRef(0); const [, setLocation] = useLocation();
  const heroVideoRefs = useRef<Array<HTMLVideoElement | null>>([]);
  const utils = trpc.useUtils(); useEditorialLive(() => { utils.editorial.featured.invalidate(); utils.media.homeBackgrounds.invalidate(); utils.media.homeBackgroundConfig.invalidate(); });
  const content = usePortalContent("Home");
  const globalContent = usePortalContent("Global");
  const hero = content.block<HeroContent>("hero", portalContentDefaults.Home.hero as HeroContent);
  const planning = content.block<HeroContent>("planning", portalContentDefaults.Home.planning as HeroContent);
  const featured = content.block<FeaturedContent>("featured", portalContentDefaults.Home.featured as FeaturedContent);
  const editorialFronts = content.block<EditorialFrontsContent>("editorialFronts", portalContentDefaults.Home.editorialFronts as unknown as EditorialFrontsContent);
  const footer = globalContent.block<{ items: Array<{ label: string; href: string; external?: boolean }>; legalItems: Array<{ label: string; href?: string }> }>("footer", portalContentDefaults.Global.footer as unknown as { items: Array<{ label: string; href: string; external?: boolean }>; legalItems: Array<{ label: string; href?: string }> });
  const heroVideos = useMemo(() => {
    if (isStaticFirebasePreview) return [{ mediaType: "vídeo" as const, assetUrl: firebasePreviewAssets.heroVideoUrl }];
    const curated = (livingBackgrounds || []).filter(item => item.mediaType === "vídeo" && item.assetUrl).slice(0, 4);
    return curated.length ? curated : [{ mediaType: "vídeo" as const, assetUrl: "/oju-assets/orixas-transicao-ritual-cinematografica.mp4" }];
  }, [livingBackgrounds]);
  const heroVideo = heroVideos[heroIndex % Math.max(heroVideos.length, 1)];
  const displaySeconds = heroTransition?.displaySeconds || 14;
  const transitionMilliseconds = heroTransition?.transitionMilliseconds || 1100;
  useEffect(() => { setHeroIndex(index => heroVideos.length ? index % heroVideos.length : 0); }, [heroVideos.length]);
  useEffect(() => {
    if (heroVideos.length < 2) return;
    const timer = window.setInterval(() => setHeroIndex(index => (index + 1) % heroVideos.length), displaySeconds * 1000);
    return () => window.clearInterval(timer);
  }, [displaySeconds, heroVideos.length]);
  const cards = items?.slice(0, 4) || [];
  const playHero = () => { heroVideoRefs.current[heroIndex]?.play(); heroVideoRefs.current[heroIndex]?.scrollIntoView({ behavior: "smooth", block: "center" }); };
  const toggleMute = () => { const nextMuted = !isMuted; heroVideoRefs.current.forEach(video => { if (video) video.muted = nextMuted; }); setIsMuted(nextMuted); }; const signalAdminEntry = () => { const now = Date.now(); const next = now - adminTapAt.current < 2200 ? adminTapCount + 1 : 1; adminTapAt.current = now; setAdminTapCount(next); if (next >= 5) { setAdminTapCount(0); setLocation("/admin"); } };
  return <div className="min-h-screen bg-[#070605] text-white"><PageMeta title="Ojú Mídia — memória negra, casa e chão" description="Documentação afro-brasileira e de religiosidades de matriz africana, com crédito, território e autorização." /><main><section className="relative min-h-[620px] overflow-hidden border-b border-white/10 bg-[#100d0a] sm:min-h-[680px]"><PublicHeader cinematic />{heroVideos.map((video, index) => <video key={video.assetUrl} ref={element => { heroVideoRefs.current[index] = element; }} autoPlay loop muted={isMuted} playsInline className="absolute inset-0 h-full w-full object-cover transition-opacity ease-out" style={{ opacity: index === heroIndex ? 1 : 0, transitionDuration: `${transitionMilliseconds}ms` }} src={video.assetUrl} />)}<div className="absolute inset-0 bg-[linear-gradient(90deg,rgba(0,0,0,.94)_0%,rgba(0,0,0,.72)_39%,rgba(0,0,0,.18)_72%,rgba(0,0,0,.5)_100%)]" /><div className="absolute inset-0 bg-[linear-gradient(0deg,rgba(0,0,0,.7),transparent_38%)]" /><div className="relative container flex min-h-[620px] items-end pb-20 pt-32 sm:min-h-[680px]">{hero && <div className="max-w-xl"><p className="text-[11px] font-bold uppercase tracking-[.14em] text-[#ef9e59]">{hero.eyebrow}</p><h1 className="mt-5 font-serif text-5xl leading-[.95] tracking-tight sm:text-7xl">{hero.title}</h1><p className="mt-6 max-w-md text-base leading-7 text-white/80">{hero.description}</p><div className="mt-8 flex flex-wrap items-center gap-5">{hero.ctaLabel && hero.ctaHref && <Link href={hero.ctaHref} className="inline-flex items-center gap-4 bg-[#ed9c58] px-6 py-4 text-xs font-bold uppercase tracking-[.08em] text-[#24140b] transition hover:bg-[#f4b273]">{hero.ctaLabel} <ArrowRight className="h-5 w-5" /></Link>}{hero.secondaryLabel && hero.secondaryHref && <Link href={hero.secondaryHref} className="inline-flex items-center gap-3 text-xs font-bold uppercase tracking-[.08em] text-white"><span className="grid h-9 w-9 place-items-center rounded-full border border-white/80"><Play className="ml-0.5 h-4 w-4" /></span>{hero.secondaryLabel}</Link>}{heroVideo?.assetUrl ? <button onClick={playHero} className="text-xs font-bold uppercase tracking-[.08em] text-white/70">Assistir miniclipe</button> : null}</div></div>}</div>{heroVideo?.assetUrl ? <div className="absolute bottom-7 right-7 flex items-center gap-3 text-white/80">{heroVideos.map((video, index) => <button key={video.assetUrl} onClick={() => setHeroIndex(index)} className={`h-2 w-2 rounded-full ${index === heroIndex ? "bg-[#ef9e59]" : "bg-white/60"}`} aria-label={`Exibir miniclipe ${index + 1}`} />)}<button onClick={toggleMute} className="ml-6 grid h-9 w-9 place-items-center rounded-full border border-white/60" aria-label={isMuted ? "Ativar som do miniclipe" : "Silenciar miniclipe"}><Volume2 className="h-4 w-4" /></button></div> : <p className="absolute bottom-7 right-7 max-w-xs text-right text-[10px] font-bold uppercase tracking-[.12em] text-white/55">Ative um miniclipe autorizado no Centro Administrativo.</p>}</section>
    {featured && <section className="container py-10 sm:py-12"><div className="mb-6 flex items-end justify-between gap-4"><div><p className="text-[10px] font-bold uppercase tracking-[.14em] text-[#ef9e59]">{featured.eyebrow}</p><h2 className="mt-2 font-serif text-4xl">{featured.title}</h2></div><Link href={featured.allHref} className="inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[.08em] text-[#ef9e59]">{featured.allLabel} <ArrowRight className="h-4 w-4" /></Link></div>{cards.length ? <div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{cards.map(item => <Link href={`/historias/${item.slug}`} key={item.id} className="group relative min-h-48 overflow-hidden rounded border border-white/10 bg-[#17130f] p-4 transition hover:border-[#ef9e59]/70">{item.coverUrl ? (item.coverType === "vídeo" ? <video muted autoPlay loop playsInline className="absolute inset-0 h-full w-full object-cover opacity-50 transition group-hover:opacity-70" src={item.coverUrl} /> : <img src={item.coverUrl} alt={item.coverCredit || item.title} className="absolute inset-0 h-full w-full object-cover opacity-50 transition group-hover:scale-105 group-hover:opacity-70" />) : <div className="absolute inset-0 bg-[radial-gradient(circle_at_75%_10%,rgba(186,110,51,.5),transparent_34%),linear-gradient(145deg,#241a12,#080706)]" />}<div className="absolute inset-0 bg-gradient-to-t from-black via-black/25 to-transparent" /><div className="relative flex h-full flex-col justify-end"><p className="text-[10px] font-bold uppercase tracking-[.12em] text-[#ef9e59]">{item.contentKind}</p><h3 className="mt-2 font-serif text-2xl leading-[1.05]">{item.title}</h3><p className="mt-3 flex items-center gap-1 text-xs text-white/75"><MapPin className="h-3.5 w-3.5" />{[item.territoryName, item.photographerName].filter(Boolean).join(" · ") || "Ver conteúdo"}</p></div></Link>)}</div> : <div className="rounded border border-dashed border-white/20 px-6 py-12 text-center text-sm text-white/60">{featured.emptyMessage}</div>}</section>}
    {editorialFronts && <section className="container grid gap-3 pb-10 md:grid-cols-3">{editorialFronts.items.map(entry => <FrontCard key={entry.href} {...entry} />)}</section>}
    <OjuMethod compact />
    {activeAds?.length ? <section className="border-y border-white/10 bg-[#0c0907]"><div className="container py-10"><div className="mb-6 flex flex-wrap items-end justify-between gap-4"><div><p className="text-[10px] font-bold uppercase tracking-[.14em] text-[#ef9e59]">Divulgação contratada</p><h2 className="mt-2 font-serif text-3xl">Serviços e parceiros</h2></div><p className="max-w-sm text-xs leading-5 text-white/55">Este espaço comercial é identificado e não integra a curadoria editorial.</p></div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{activeAds.map(ad => <article key={ad.id} className="overflow-hidden rounded border border-white/15 bg-[#17130f]">{ad.mediaUrl && (ad.mediaType === "vídeo" ? <video muted autoPlay loop playsInline className="aspect-[16/9] w-full object-cover" src={ad.mediaUrl} /> : <img className="aspect-[16/9] w-full object-cover" src={ad.mediaUrl} alt={ad.title} />)}<div className="p-4"><p className="text-[10px] font-bold uppercase tracking-[.12em] text-[#ef9e59]">Divulgação contratada · {ad.format}</p><h3 className="mt-3 font-serif text-2xl">{ad.title}</h3>{ad.description && <p className="mt-2 line-clamp-2 text-sm leading-6 text-white/65">{ad.description}</p>}<a href={ad.contact.startsWith("http") ? ad.contact : `https://wa.me/${ad.contact.replace(/\D/g, "")}`} target="_blank" rel="noreferrer" className="mt-5 inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[.08em] text-[#ef9e59]">Entrar em contato <ArrowRight className="h-4 w-4" /></a></div></article>)}</div></div></section> : null}
    {planning && <section className="container pb-10 pt-10"><div className="grid gap-6 border border-[#4b2d19] bg-[linear-gradient(100deg,#26180f,#51301d,#1b120d)] p-6 sm:grid-cols-[auto_1fr_auto] sm:items-center"><Camera className="h-12 w-12 text-[#ef9e59]" /><div><h2 className="font-serif text-3xl">{planning.title}</h2><p className="mt-2 max-w-xl text-sm leading-6 text-white/75">{planning.description}</p></div>{planning.ctaLabel && planning.ctaHref && <Link href={planning.ctaHref} className="inline-flex items-center justify-center gap-3 bg-[#ed9c58] px-6 py-4 text-xs font-bold uppercase tracking-[.08em] text-[#24140b]">{planning.ctaLabel} <ArrowRight className="h-5 w-5" /></Link>}</div></section>}
  </main><footer className="border-t border-white/10 bg-black"><div className="container flex flex-wrap items-center justify-between gap-6 py-8 text-[10px] font-bold uppercase tracking-[.08em] text-white/60"><OjuMark compact cinematic onClick={signalAdminEntry} />{footer && <><div className="flex flex-wrap gap-5">{footer.items.map(item => item.external ? <a key={item.href} href={item.href} target="_blank" rel="noreferrer">{item.label}</a> : <Link key={item.href} href={item.href}>{item.label}</Link>)}</div><div className="flex gap-4">{footer.legalItems.map(item => item.href ? <Link key={item.label} href={item.href}>{item.label}</Link> : <span key={item.label}>{item.label}</span>)}</div></>}</div></footer></div>;
}
