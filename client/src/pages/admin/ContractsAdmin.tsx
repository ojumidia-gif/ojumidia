import { Button } from "@/components/ui/button";
import { FileSignature } from "lucide-react";
import { Link } from "wouter";
import { trpc } from "@/lib/trpc";
import { AdminPage, EmptyAdmin } from "./_shared";

const statusStyles: Record<string, string> = { Rascunho: "bg-[#eee9dc] text-[#655e52]", Enviado: "bg-[#e1eff3] text-[#1f5f72]", Assinado: "bg-[#e6f2e5] text-[#496b3b]", Arquivado: "bg-[#eee9dc] text-[#655e52]" };
export default function ContractsAdmin() { const { data, isLoading } = trpc.commercial.listCoverageContracts.useQuery(undefined, { refetchInterval: 5000 }); return <AdminPage eyebrow="Contratos" title="Divulgação institucional documentada." action={null}><section className="admin-card p-6"><div className="flex gap-3"><div className="rounded-xl bg-[#f6d978] p-3"><FileSignature className="h-5 w-5" /></div><div><p className="font-serif text-2xl">Contratos de divulgação</p><p className="mt-1 max-w-3xl text-sm leading-6 text-[#655e52]">Cada contrato permanece no Centro Administrativo, vinculado à sua Cobertura institucional. Nenhum documento é exibido no portal público.</p></div></div>{isLoading ? <p className="mt-6 text-sm">Carregando contratos...</p> : data?.length ? <div className="mt-6 divide-y divide-[#242017]/10">{data.map(contract => <article className="grid gap-4 py-5 sm:grid-cols-[1fr_auto] sm:items-center" key={contract.id}><div><p className="font-medium">{contract.contractor}</p><p className="mt-1 text-xs text-[#655e52]">Cobertura #{contract.publicationId} · criado em {new Date(contract.createdAt).toLocaleDateString("pt-BR")}</p><p className="mt-2 break-all text-xs text-[#806817]">{contract.documentUrl}</p></div><div className="flex items-center gap-3"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${statusStyles[contract.status]}`}>{contract.status}</span><Button asChild size="sm" variant="outline"><Link href={`/admin/editar/${contract.publicationId}`}>Abrir Cobertura</Link></Button></div></article>)}</div> : <div className="mt-6"><EmptyAdmin text="Os contratos surgem quando uma Cobertura institucional é vinculada a uma solicitação contratada." /></div>}</section></AdminPage>; }
