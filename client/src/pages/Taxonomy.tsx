import { ArrowRight, MapPin } from "lucide-react";
import { Link, useRoute } from "wouter";
import { PublicHeader } from "@/components/PublicHeader";
import { trpc } from "@/lib/trpc";

export default function Taxonomy() { const [, params] = useRoute("/territorios/:slug"); const { data: taxonomies, isLoading: loadingTaxonomies } = trpc.editorial.taxonomies.useQuery(); const territory = taxonomies?.find(item => item.slug === params?.slug); const { data: contents, isLoading: loadingContents } = trpc.editorial.search.useQuery({ territoryId: territory?.id, limit: 24, offset: 0 }, { enabled: Boolean(territory?.id), refetchInterval: 30000 }); return <div className="min-h-screen bg-[#070605] text-white"><PublicHeader cinematic /><main className="container pb-16 pt-32">{loadingTaxonomies ? <p className="text-white/60">Carregando território...</p> : !territory ? <section><p className="text-[10px] font-bold uppercase tracking-[.14em] text-[#ef9e59]">Territórios</p><h1 className="mt-5 font-serif text-5xl">Território indisponível.</h1><Link href="/territorios" className="mt-8 inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[.08em] text-[#ef9e59]">Ver todos os territórios <ArrowRight className="h-4 w-4" /></Link></section> : <><section className="grid gap-8 lg:grid-cols-[1.15fr_.85fr]"><div><p className="text-[10px] font-bold uppercase tracking-[.14em] text-[#ef9e59]">Território documental</p><h1 className="mt-5 font-serif text-6xl leading-[.95] sm:text-7xl">{territory.name}</h1><p className="mt-7 max-w-2xl text-base leading-7 text-white/65">Este território reúne histórias, coberturas, documentos visuais e relações que ajudam a preservar a memória de seus encontros e transformações.</p></div><aside className="rounded border border-white/15 bg-[#100d0a] p-6"><p className="text-[10px] font-bold uppercase tracking-[.14em] text-[#ef9e59]">Navegação documental</p><p className="mt-5 font-serif text-2xl">Conteúdos relacionados ao território.</p><p className="mt-3 text-sm leading-6 text-white/60">A relação é construída pela curadoria editorial e pode reunir texto, fotografias, vídeos e miniclipes quando publicados.</p></aside></section><section className="mt-14"><div className="flex items-end justify-between gap-4"><div><p className="text-[10px] font-bold uppercase tracking-[.14em] text-[#ef9e59]">Acervo relacionado</p><h2 className="mt-2 font-serif text-4xl">Histórias deste território</h2></div><MapPin className="h-6 w-6 text-[#ef9e59]" /></div>{loadingContents ? <p className="mt-8 text-white/60">Organizando conteúdos...</p> : contents?.items.length ? <div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{contents.items.map((content, index) => <Link href={`/historias/${content.slug}`} key={content.id} className="group relative min-h-64 overflow-hidden rounded border border-white/15 bg-[#100d0a] p-5 transition hover:border-[#ef9e59]/80"><div className={`absolute inset-0 ${index % 2 ? "bg-[linear-gradient(135deg,#17110d,#744321,#070605)]" : "bg-[radial-gradient(circle_at_80%_20%,rgba(201,125,61,.45),transparent_26%),linear-gradient(135deg,#17110d,#070605)]"}`} /><div className="absolute inset-0 bg-gradient-to-t from-black via-black/25 to-transparent" /><div className="relative flex h-full flex-col justify-end"><p className="text-[10px] font-bold uppercase tracking-[.14em] text-[#ef9e59]">{content.contentKind}</p><h3 className="mt-3 font-serif text-3xl leading-[1.05]">{content.title}</h3><p className="mt-3 line-clamp-2 text-sm leading-6 text-white/65">{content.summary}</p><span className="mt-5 inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[.08em] text-[#ef9e59]">Abrir conteúdo <ArrowRight className="h-4 w-4" /></span></div></Link>)}</div> : <div className="mt-8 rounded border border-dashed border-white/20 bg-[#100d0a] p-10"><p className="font-serif text-3xl">Este território ainda aguarda seus primeiros registros.</p><p className="mt-3 max-w-xl text-sm leading-6 text-white/60">Assim que a equipe relacionar e publicar conteúdos, eles aparecerão nesta página com suas mídias e contextos documentais.</p></div>}</section></>}</main></div>; }
