import { PublicHeader } from "@/components/PublicHeader";
import { ArrowLeft, MapPin } from "lucide-react";
import { Link, useRoute } from "wouter";
import { trpc } from "@/lib/trpc";
import { useEditorialLive } from "@/hooks/useEditorialLive";

export default function Story() {
  const [, params] = useRoute("/historias/:slug");
  const { data, isLoading } = trpc.editorial.bySlug.useQuery({ slug: params?.slug || "" }, { enabled: Boolean(params?.slug), refetchInterval: 30000 });
  const utils = trpc.useUtils(); useEditorialLive(() => { utils.editorial.bySlug.invalidate(); });
  if (isLoading) return <div className="min-h-screen bg-[#070605] text-white"><PublicHeader cinematic /><main className="container pt-32">Carregando conteúdo...</main></div>;
  if (!data) return <div className="min-h-screen bg-[#070605] text-white"><PublicHeader cinematic /><main className="container pt-32"><h1 className="font-serif text-4xl">Conteúdo indisponível.</h1><Link href="/" className="mt-6 inline-flex gap-2 text-sm font-semibold text-[#ef9e59]"><ArrowLeft className="h-4 w-4" />Voltar ao portal</Link></main></div>;
  const cover = data.media?.[0]; const remainingMedia = data.media?.slice(1) || [];
  return <div className="min-h-screen bg-[#070605] text-white"><PublicHeader cinematic /><article><section className="relative overflow-hidden border-b border-white/10 bg-[#100d0a] pt-32"><div className="container grid gap-10 py-14 lg:grid-cols-[1fr_.9fr]"><div className="self-end"><p className="text-[10px] font-bold uppercase tracking-[.14em] text-[#ef9e59]">{data.contentKind}</p><h1 className="mt-5 max-w-3xl font-serif text-5xl leading-[.95] sm:text-7xl">{data.title}</h1>{data.subtitle && <p className="mt-6 max-w-2xl font-serif text-2xl leading-snug text-white/70">{data.subtitle}</p>}<p className="mt-8 flex items-center gap-2 text-xs font-bold uppercase tracking-[.1em] text-white/55"><MapPin className="h-4 w-4 text-[#ef9e59]" />Publicado em {data.publishedAt ? new Date(data.publishedAt).toLocaleDateString("pt-BR") : "preparação editorial"}</p></div>{cover && <figure className="overflow-hidden rounded border border-white/15 bg-black">{cover.mediaType === "vídeo" ? <video controls className="aspect-video w-full" src={cover.assetUrl} /> : <img src={cover.assetUrl} alt={cover.credit || data.title} className="aspect-[16/10] w-full object-cover" />}<figcaption className="px-4 py-3 text-xs text-white/55">{cover.credit}{cover.origin ? ` · ${cover.origin}` : ""}</figcaption><Link href={`/licenciar-midia?mid=${cover.id}`} className="block border-t border-white/10 px-4 py-3 text-[10px] font-bold uppercase tracking-[.12em] text-[#ef9e59]">Solicitar uso desta mídia</Link></figure>}</div></section><section className="container max-w-4xl py-12 sm:py-16">{data.editorialAuthorization?.authorized && <aside className="border-l-2 border-[#6fbd77] bg-[#152518] px-5 py-4"><p className="text-xs font-bold uppercase tracking-[.16em] text-[#8dda95]">Cobertura autorizada editorialmente</p><p className="mt-2 text-sm leading-6 text-white/65">A contratação permanece privada; somente o conteúdo autorizado integra o acervo.</p></aside>}{data.sponsored && <aside className="mt-4 border-l-2 border-[#ef9e59] bg-[#2a1a10] px-5 py-4"><p className="text-xs font-bold uppercase tracking-[.16em] text-[#ef9e59]">Divulgação contratada</p><p className="mt-2 text-sm leading-6 text-white/65">{data.sponsorDisclosure || "Conteúdo identificado com transparência pela Ojú Mídia."}</p></aside>}{data.taxonomies?.length ? <section className="mt-8"><p className="text-[10px] font-bold uppercase tracking-[.16em] text-[#ef9e59]">Mapa documental</p><div className="mt-3 flex flex-wrap gap-2">{data.taxonomies.map(item => <span key={item.id} className="rounded-full border border-white/15 px-3 py-1 text-xs text-white/70">{item.dimension} · {item.name}</span>)}</div></section> : null}<div className="mt-10 whitespace-pre-wrap font-serif text-xl leading-9 text-white/85">{data.body || data.summary}</div>{remainingMedia.length > 0 && <section className="mt-14"><p className="text-[10px] font-bold uppercase tracking-[.16em] text-[#ef9e59]">Materiais da cobertura</p><div className="mt-5 grid gap-5">{remainingMedia.map(media => <figure key={media.id} className="overflow-hidden rounded border border-white/15 bg-[#0e0b09]">{media.mediaType === "vídeo" ? <video controls className="aspect-video w-full" src={media.assetUrl} /> : <img src={media.assetUrl} alt={media.credit || data.title} className="w-full" />}<figcaption className="px-4 py-3 text-xs text-white/55">{media.credit}{media.origin ? ` · ${media.origin}` : ""}</figcaption><Link href={`/licenciar-midia?mid=${media.id}`} className="block border-t border-white/10 px-4 py-3 text-[10px] font-bold uppercase tracking-[.12em] text-[#ef9e59]">Solicitar uso desta mídia</Link></figure>)}</div></section>}</section></article></div>;
}
